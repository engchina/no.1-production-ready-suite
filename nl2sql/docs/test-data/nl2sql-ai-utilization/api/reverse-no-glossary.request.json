{
  "sql": "SELECT CUSTOMER_ID, SUM(TOTAL_AMOUNT) AS TOTAL_AMOUNT FROM APP.ORDERS GROUP BY CUSTOMER_ID ORDER BY TOTAL_AMOUNT DESC FETCH FIRST 5 ROWS ONLY",
  "profile_id": "default",
  "use_glossary": false
}

