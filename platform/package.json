{
  "name": "@engchina/production-ready-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Shared frontend (UI) + backend (Python) single source of truth for the No.1 Production Ready product family (RAG / NL2SQL / Agent). The Python backend_core package is managed separately with uv under packages/backend_core.",
  "workspaces": [
    "packages/ui"
  ],
  "scripts": {
    "build": "npm run build -w @engchina/production-ready-ui",
    "test": "npm run test -w @engchina/production-ready-ui",
    "typecheck": "npm run typecheck -w @engchina/production-ready-ui"
  }
}
