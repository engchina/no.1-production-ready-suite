# 製品固有の設定（接頭辞は製品名。例: RAG_ / NL2SQL_ / AGENT_）。
# 3製品共通の設定（OCI / Oracle / アップロード保存先 / モデル / 共通認証）は platform/.env.example（PLATFORM_*）。
SERVICE_APP_VERSION=0.1.0
SERVICE_LOG_LEVEL=INFO
SERVICE_ENVIRONMENT=local
SERVICE_CORS_ORIGINS=http://localhost:3000
