# バックエンド API のベース URL（Vite dev/preview proxy と Docker nginx proxy で使用）
# Vite の config は .env.local を読まないため、dev ではシェルで渡す: BACKEND_URL=http://localhost:8000 npm run dev
# 未指定の Vite dev/preview は /api を proxy せず 404 を返す（hermetic）。
BACKEND_URL=http://localhost:8000
