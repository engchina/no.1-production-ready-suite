"""回答評価（LLM 採点）が使う判定基準。回答生成・CRAG 判定は結合しない (#957 / #960 / #967)。

各定数は評価 prompt の節 2 に結合される。文は「どの説明を不支持・不足・誤適用と判定するか」の形で書き、
回答の書き方（表記・節構成）は指示しない。
"""

DOCUMENT_VALUE_POLICY = """文書の例示値と今回の実データの区別:
- 画面キャプチャ・Vision 生成説明に見える値（年月・金額・氏名・番号・コード・選択状態）は文書の例示であり、今回の実データではない。表示されているだけで現在値・既定値・必須値・許容範囲・登録済み状態と断定した説明は誤り。
- 画面例の値に触れる説明は「画面例では〇〇」と書き、実際の値として案内できるのは本文が「〜と入力します」のように指定している場合か、利用者が質問で明示した場合だけ。
- 本文で明示された既定値・入力形式・許容範囲・条件付き規則は根拠として認める。例示値があることを理由に操作説明を拒否した回答は不足。
"""

OPERATION_GUIDANCE_POLICY = DOCUMENT_VALUE_POLICY + """
操作・確認案内の充足基準:
- 集計値（count）・構成明細（members）・算定根拠（calculation_basis）は別要求として照合する。候補集合と最終集計対象の一致を推測した説明は不支持、「のみ」の出力制限・条件・基準年月を落とした説明は不足。
- 本人と関係者、同名の別業務、独立した状態項目、似た名称の項目は原文どおり区別する。検索0件・エラー・実行例から原因や成功を確定した説明は不支持。資料未記載は機能不存在の証拠ではない。
- 希望する結果・観測済み状態・試した操作・利用者の仮説を分ける。仮説の手段を目的の前提にした説明や過去の操作経路を必須経路にした説明は目的不一致。利用者が申告した状態を未確認に戻し、再確認手順で質問の代わりにした回答は不足。
- 定義・項目の意味を尋ねる質問は、各項目の意味と表示条件を直接答えていれば足りる。確認手順の自動追加は加点しない。
- 根拠で支持できる説明を先にし、閲覧する実データ・資料とその目的、閲覧後に判断する事項を分けて示していれば、個案の最終判断が未確定でも目的不一致にしない。「資料を追加してください」だけの案内、名称・所在・URL・内容の創作は不足・不支持。
- 実値の未確認と文書根拠の不足を分ける。操作自体の根拠がないのに手順・設定値・計算規則を推測した説明は不支持。
"""

SECTION_APPLICABILITY_POLICY = """ページ内の節・処理条件の適用性:
- 質問の業務対象・変更された項目・発生条件と、候補の画面名・節見出し・処理区分・本文・前提条件を照合する。同じ業務名や「番号」「変更」の共通語だけで適用可能とした説明は不支持。異なる項目の変更を前提とする手順を今回の手順にした回答は誤適用。適用条件が一致する手順を一律に拒否した回答は不足。
- 同じページに複数の処理区分がある場合、区分ごとの対象・条件・操作・結果を保持する。片方の条件や効果を他方へ移した説明は不支持。
"""

OPERATION_BINDING_POLICY = SECTION_APPLICABILITY_POLICY + """
操作入口と対象の結び付け:
- 項目名・ボタン・キー番号・操作効果を別々に原文と照合する。似た名称（「コード」「顧客番号」「顧客コード」）の同一視、別機能の検索欄・ボタンの借用、メニュー分類と実際の機能の混同は不支持。
- 操作の対象・前提条件・操作内容・期待効果のそれぞれに同じ機能の根拠が要る。対象と機能の対応が未確認なのに適用可能と断定した説明、関連機能の存在だけでエラー解消・番号の解放・成功を保証した説明は不支持。
- 根拠にない保存・確認・プレビュー・編集・印刷・再発行の操作を補った説明（「必要に応じて」と付けても同じ）は不支持。原文の確定・完了操作を落とした手順は不足。
- 帳票ID・最大行数・ボタン名は根拠に裏付けられた場合だけ認める。名称や出典が別々に存在するだけでは対応の証明にならない。
"""
